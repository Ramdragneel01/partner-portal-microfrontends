// Generated by GitHub Copilot
/**
 * ViewConfig TypeScript types — README §8 Config-Driven UI System.
 * Views are JSON configurations, not React code.
 * @module @shared/types/view-config
 */

/** Supported component renderers */
export type ViewComponentType = 'chart' | 'table' | 'text' | 'kpi' | 'form' | 'summary' | 'list';

/** HTTP methods for data sources */
export type DataSourceMethod = 'GET' | 'POST' | 'PUT' | 'DELETE';

/** Data source — API call configuration */
export interface DataSource {
    id?: string;
    purpose?: 'data' | 'filters' | 'actions' | 'metadata';
    method: DataSourceMethod;
    path: string;
    params?: Record<string, unknown>;
    body?: Record<string, unknown>;
    headers?: Record<string, string>;
    /** JMESPath expression to transform raw API response */
    responseTransform?: string;
    disableCache?: boolean;
    inheritFilters?: boolean;
    errorHandling?: 'default' | 'component' | 'ignore';
}

/** Maps API response fields → component props */
export interface DataBinding {
    bindings: Record<string, string | DataBindingConfig>;
    strategy?: 'merge' | 'replace' | 'overlay';
}

export interface DataBindingConfig {
    /** JMESPath or JSONata expression */
    path: string;
    engine?: 'jmespath' | 'jsonata';
    transform?: string;
    fallback?: unknown;
}

/* ─── Layout Nodes ───────────────────────────────────────── */

export interface GridColumnNode {
    /** 1–12 column span */
    size: number | string | Record<string, number | string>;
    children?: LayoutNode[];
}

export interface GridLayoutNode {
    type: 'grid';
    rows: Array<{
        columns: GridColumnNode[];
    }>;
}

export interface StackLayoutNode {
    type: 'stack';
    direction?: 'row' | 'column';
    children: LayoutNode[];
}

export interface ComponentLayoutNode {
    type: 'component';
    id: string;
}

export interface TabDefinition {
    name: string;
    icon?: string;
    disabled?: boolean;
    badge?: string | number;
    layout: LayoutNode;
}

export interface TabLayoutNode {
    type: 'tab';
    tabs: TabDefinition[];
    defaultTab?: number;
}

export type LayoutNode = GridLayoutNode | StackLayoutNode | ComponentLayoutNode | TabLayoutNode;

/* ─── Actions ────────────────────────────────────────────── */

export type ActionTrigger = 'click' | 'doubleClick' | 'rightClick' | 'success' | 'error' | 'change';

export interface NavigationAction {
    id: string;
    title: string;
    type: 'navigation';
    trigger: ActionTrigger;
    target?: 'row' | 'cell' | 'component';
    navigation: {
        viewId?: string;
        presentation?: 'fullPage' | 'modal' | 'slideout';
        dimensions?: { width?: string | number; height?: string | number };
        url?: string;
        openInNewTab?: boolean;
    };
}

export interface EntityCrudAction {
    id: string;
    title: string;
    type: 'entity-crud';
    trigger: 'click';
    form: { inputSchema: Record<string, unknown> };
    endpoint: { method: string; path: string };
    onSuccess?: unknown;
    onError?: unknown;
}

export type ViewAction = NavigationAction | EntityCrudAction;

/* ─── ViewComponent ──────────────────────────────────────── */

export interface ViewComponent {
    type: ViewComponentType;
    id: string;
    title?: string;
    description?: string;
    props?: Record<string, unknown>;
    dataSource?: DataSource | DataSource[];
    dataBinding?: DataBinding;
    actions?: ViewAction[];
    filters?: unknown[];
    metadata?: {
        hideTitle?: boolean;
        hideDescription?: boolean;
        hideCard?: boolean;
    };
}

/* ─── ViewConfig ─────────────────────────────────────────── */

export interface ViewConfig {
    id: string;
    tenantId: string;
    title: string;
    description?: string;
    layout?: LayoutNode;
    components: ViewComponent[];
    actions?: ViewAction[];
    filters?: unknown[];
    dataSource?: DataSource | DataSource[];
    dataBinding?: DataBinding;
}
